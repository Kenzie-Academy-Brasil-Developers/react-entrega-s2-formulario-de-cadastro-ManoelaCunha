import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  root: { margin: "0px 20px" },
  textfield: {
    width: 250,
  },
  button: {
    margin: "20px 0px",
  },
  icone: {
    fontSize: "20px",
    cursor: "pointer",
    color: "gray",
  },
}));
